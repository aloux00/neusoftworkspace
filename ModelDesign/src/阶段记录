AsbSwingFactory:整体采用工厂方法模式，抽象工厂AsbSwingFactory有多个子类，当前只有一个能获取普通
主窗口和普通空白窗口的子类，方便以后拓展可以获取特殊窗口。

AbsJFrame:抽象AbsJFrame封装了JFrame,
为了在工厂中执行较少的代码就能初始化窗口，能有更多的 精力在窗口功能的多样化上。

AbsButton:AbsButton简单封装了JButton(其实就是给设置了初始大小)，并追加了监听接口，
并在构造函数中完成了对自己的监听，具体实现由子类完成。所以，组件包我打算在每个功能窗口下搞
一个专门盛放该窗口组件，毕竟按钮和他的功能实现绑定在了一起，哈哈，无奈之举。 